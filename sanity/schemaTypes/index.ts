import { type SchemaTypeDefinition } from "sanity";

export const schema: { types: SchemaTypeDefinition[] } = {
  // Sanity auth/login collections have been removed.
  // Keep this array for Studio bootstrapping; add non-auth schemas here if needed.
  types: [],
};
